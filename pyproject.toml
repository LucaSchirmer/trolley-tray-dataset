[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trolley-tray-dataset"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = []

[tool.pytest.ini_options]
testpaths = ["automatic_masks/tests"]
addopts = "-q"
